[color]
    ui = true
[push]
    default = current
[core]
    whitespace = -trailing-space
    whitespace = trailing-space
    whitespace = trailing-space
[alias]
    log = log --stat
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red
    new = green
[color "status"]
    added = yellow
    changed = green
    untracked = cyan

